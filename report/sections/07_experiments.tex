\section{Experiments and validation}
% TODO: fill with data from experiments/results/.
% Content plan:
%  - Methodology: same scenario, >=5 runs per strategy, mean +/- std
%    (spawns are random). Harness: per map, start the server with
%    GAME_NAME=26c1_N, then scripts/run-baseline.js runs every strategy
%    x N fresh-identity runs; scripts/aggregate-results.js (core in
%    src/metrics/aggregate.js) produces the comparison table / CSV.
%  - Note the per-map config differences that make strategy choice
%    matter (from assets/games/26c1_*.json): decay 1s vs 2s, reward 30
%    vs 50, capacity 20 vs 5 on maps 4-5, intelligent NPC on maps 4-5,
%    one-way arrows on map 5.
%  - RESULT (baseline-v1, experiments/RESULTS-baseline-v1.md): greedy-nearest
%    wins outright on 6/8 maps and ties on 26c1_3 and 26c1_5; it is never
%    beaten. The "slow-decay favours batching" prediction for 26c1_7/8 was
%    FALSIFIED -- delivery-threshold does worst there. Diagnosed cause
%    (step 3a, from run logs): greedy hoards (~7.6 parcels/delivery, delivers
%    only when nothing reachable) and so out-collects the value-aware
%    strategies, which all small-batch (~2/delivery); no real plan failures.
%    Honesty note worth including: the apparent mission-aware vs
%    reward-distance gap on 26c1_2/4 was small-sample noise (code + logs show
%    they are identical with no mission).
%  - Preliminary validation data already available (2026-06-11, local
%    server, empty_10, 60 s): reward-distance scored 800 with 30
%    deliveries; the live-smoke vs live-smoke2 pair quantifies the
%    belief-reconciliation fix (see section 3 and
%    experiments/README.md). Usable as a worked example of how the
%    metrics pipeline supports debugging, not only benchmarking.
%  - Challenge 1 table: per-map final scores per strategy
%    (booktabs table; data from experiments/results/*.json).
%  - Score-over-time plots for the most interesting maps
%    (figures from notebooks/).
%  - Challenge 2: per-mission outcomes (bonus obtained? compliance rate?).
%  - PDDL on/off comparison.
%  - Brief failure analysis: pickupsLost, failedMoves, intention churn.
