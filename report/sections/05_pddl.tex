\section{PDDL planning}
% TODO: fill after PDDL experiments. (PDDL use and integration = 20%.)
% Content plan (the exam asks to identify WHERE and WHEN PDDL helps):
%  - Where: PDDL as a plan-library member serving the go_to intention;
%    the domain (move actions on a directed tile graph plus pickup/putdown
%    for one-parcel delivery problems) and how arrow tiles are encoded by
%    omitting edges.
%  - When: deterministic, fully-believed subproblems; why the outer loop
%    is a poor fit (per-frame world changes, online solver latency).
%  - Problem generation from beliefs (reachable region, size bound,
%    parcel-at/delivery predicates, delivered goal).
%  - Measured comparison: BFS plan vs PDDL plan (latency, plan length,
%    failure rate) from plannerCalls/plannerFailures logs.
%  - Discussion of problem complexity, as required.
%  - Design alternative / scope (DECISION): the domain now also models a
%    full single-parcel collect-and-deliver subproblem (pickup, putdown,
%    delivery), available as an optional path gated by PDDL_DELIVERY_ENABLED
%    (off by default). We keep the go_to level as the default runtime use of
%    PDDL because the environment is highly dynamic (long plans go stale
%    within seconds) and the online solver adds latency; task selection stays
%    with the BDI strategy, while PDDL provides a meaningful but bounded
%    planning component. Capacity / mission constraints (e.g. deliver-exactly,
%    value threshold) are still enforced by the BDI plans, not by PDDL.
%  - TODO(optional): crate pushing / dynamic obstacle variants
%    (the agent assumes a static known map + partial sensing).
