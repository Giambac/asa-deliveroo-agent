\section{Agent A: the BDI agent}
% TODO: fill after Challenge 1 validation. (BDI quality = 30% of the grade.)
% Content plan:
%  - Belief model: fields, timestamps, decay projection, negative
%    evidence on re-observed tiles (lab2 principles).
%  - Belief reconciliation (REAL FINDING, tell this story): live testing
%    on 2026-06-11 exposed that server ack shapes differ from the SDK
%    types (pickup/putdown acks without an `id` field). Phantom carry
%    beliefs caused 336 futile putdown retries in one 60 s run.
%    Fix: normalizeIdList + markTilePickedUp/clearCarried — a concrete
%    instance of lab2's "my knowledge was wrong, not the world" case.
%    Before/after data: experiments/results/live-smoke*.json
%    (score 173 -> 800, deliveries 7 -> 30 on the same map/duration).
%  - Options and utilities; the strategies implemented and their
%    intuition (greedy-nearest baseline, reward-distance, delivery-threshold).
%  - Strategy evaluation (REAL RESULT, retrospective; no runtime default was
%    changed after the fact): in the baseline (8 maps x 4 strategies x 5 runs,
%    experiments/RESULTS-baseline-v1.md) greedy-nearest emerges as the
%    strongest baseline -- wins or ties on all maps -- and is the reference
%    baseline in the retrospective Challenge 1 analysis (run with
%    --strategy greedy-nearest). The
%    value-aware strategies are not buggy, they just deliver in ~2-parcel
%    micro-batches while greedy hoards (~7.6/delivery) and wins on throughput.
%    Good critical-thinking material: a falsified "slow-decay favours
%    batching" hypothesis and a corrected small-sample false positive
%    (mission-aware was thought to differ from reward-distance with no
%    mission; code + logs prove they are identical -> the gap was noise).
%  - Intention revision: replace policy with additive hysteresis;
%    stop conditions (achieved / impossible / not worthwhile).
%  - Plan library and failure handling (move retries, soft-blocked
%    tiles, plan fallback).
%  - Validation on Challenge 1 maps, including the directional-arrow
%    maps (26c1_4/5) and the omniscient NPC competitor.
