\section{Architecture}
% TODO: fill once the infrastructure is stable.
% Content plan:
%  - Component diagram: BeliefBase, OptionGenerator, Strategy,
%    IntentionRevision, PlanLibrary, ActionExecutor, PathPlanner,
%    PddlPlanner, TeamProtocol, MissionInterpreter.
%  - The control loop (observe -> revise beliefs -> options -> intention
%    revision -> plan -> act) and where each module sits.
%  - Design principles: infrastructure vs. strategy separation,
%    deterministic low-level movement, LLM kept high-level,
%    serialized actions (server ActionMutex).
%  - Repository layout table mapping folders to exam requirements.
%  - Verification tooling: the offline smoke test (npm test, no
%    server/network) that pins graph semantics (arrow tiles), BFS,
%    belief revision, strategy selection, mission parsing, PDDL
%    problem generation and ack normalization.
%  - Robustness note: never trust a single server ack shape — the
%    running course servers may differ from the SDK type declarations
%    (verified live); all ack ids go through a normalization layer.
